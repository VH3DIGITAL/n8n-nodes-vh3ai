---
description: n8n community package manifest and verification requirements
globs: package.json
alwaysApply: false
---

# n8n community package manifest

When editing `package.json` for this repo:

## `n8n.n8nNodesApiVersion` (Cloud verification)

- **Must be a positive integer**: `1` or `2` only — never a float (`1.1`), string (`"1"`), or semver.
- This package uses programmatic nodes (`execute()`). Use **`1`** unless migrating to n8n's newer declarative node builder (then `2`).
- `eslint-plugin-n8n-nodes-base` only checks "is a number"; n8n Cloud's `@n8n/scan-community-package` is stricter.

```json
"n8n": {
  "n8nNodesApiVersion": 1
}
```

## Before release

1. `npm run lint` — TypeScript + credentials
2. `npm run lint:manifest` — local manifest checks (integer API version, nodes/credentials paths)
3. After publish: `npx @n8n/scan-community-package n8n-nodes-vh3ai@<version>`

## Other manifest requirements

- `keywords` must include `n8n-community-node-package`
- `n8n.nodes` and `n8n.credentials` must point at built files under `dist/`
- No runtime `dependencies`; publish via GitHub Actions with npm provenance
